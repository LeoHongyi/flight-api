spring:
  application:
    name: flightapi
  datasource:
    url: jdbc:mysql://localhost:3306/flight
    username: root
    password: wenhua630677308
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  file:
    name: flightapi.log
    path: logs
jwt:
  secret: yourSecretKeyHereShouldBeAtLeast32CharactersLong
  expiration: 86400000
